FROM UPSTREAM_REPO

RUN apk update && apk add git bash

# Add vendorcheck for vendoring checks
RUN GOPATH=$GOPATH go get github.com/FiloSottile/vendorcheck
RUN GOPATH=$GOPATH go install github.com/FiloSottile/vendorcheck

